{{#if isLoggedIn}}
<header class="header">
    <div class="header-content">
        <div class="header-logo">
            <a href="/" style="text-decoration: none;">
                <img src="images/worldpeace.png" alt="Cuisine icon" class="header-logo-image">
            </a>
        </div>
        <div class="header-title">
            <a href="/" style="text-decoration: none;">
                <h1>World Peas</h1>
            </a>
        </div>
        <form class="header-form" method="get" action="/search">
            <input type="text" id="search" name="search">
            <button type="submit" id="searchbtn">
                <img src="fpicons/searchicon.png" alt="Search icon">
            </button>
        </form>
    </div>
    <div class="header-nav">
        {{#if (eq user.type "admin")}}
        <a href="/register_admin" class="header-userprofile">Register Admin</a>
        <a href="/audit" class="header-userprofile">Audit Logs</a>
        <a href="/establishments" class="header-userprofile">Manage Establishments</a>
        {{/if}}
        <a href="/profile?user={{user.username}}" class="header-userprofile">User Profile</a>
        <a href="/logout" class="header-signout">Sign out</a>
    </div>
</header>
{{else}}
<header class="header">
    <div class="header-content">
        <div class="header-logo">
            <a href="/" style="text-decoration: none;">
                <img src="images/worldpeace.png" alt="Cuisine icon" class="header-logo-image">
            </a>
        </div>
        <div class="header-title">
            <a href="/" style="text-decoration: none;">
                <h1>World Peas</h1>
            </a>
        </div>
        <form class="header-form" method="get" action="/search">
            <input type="text" id="search" name="search">
            <button type="submit" id="searchbtn">
                <img src="fpicons/searchicon.png" alt="Search icon">
            </button>
        </form>
    </div>
    <div class="header-nav">
        <button onclick="showModal('registerModal')" id="header-signup">Sign up</button>
        <button onclick="showModal('loginModal')" id="header-login">Log in</button>
    </div>
</header>

<div id="loginModal" class="modal">
    <div class="modal-content">
        <span class="headerclose" onclick="closeModal('loginModal')">&times;</span>
        <form id="loginForm" action="/login" method="POST">
            <h1 style="text-align: center;">Login</h1>
            {{#if error_msg.[0]}}
            <div class="message-overlay" status="error" style="margin-bottom:10px;">
                <div>
                    <h6 class="red-text text-darken-2"><strong>{{error_msg.[0]}}</strong></h6>
                </div>
            </div>
            {{/if}}
            <input type="text" id="loginUsername" name="username" class="textbox-border" placeholder="Username"
                required value="{{username}}">
            <input type="password" id="loginPassword" name="password" class="textbox-border" placeholder="Password"
                required>
            <div class="rememberMeLabel">
                <label>Remember Me:<input type="checkbox" id="rememberMe" name="rememberMe"></label>
            </div>
            <button type="submit">Login Now</button>
        </form>
    </div>
</div>

<div id="registerModal" class="modal">
    <div class="modal-content2">
        <span class="headerclose" onclick="closeModal('registerModal')">&times;</span>
        <form id="registerForm" enctype="multipart/form-data" action="/register" method="POST" onsubmit="return validateRegisterForm()">
            <h1 style="text-align: center;">Sign up</h1>
            <input type="text" id="registerUsername" name="username" class="textbox-border" placeholder="Username"
                required>
            <div class="password-input-container">
                <input type="password" id="registerPassword" name="password" class="textbox-border" placeholder="Password"
                    required>
                <button type="button" class="toggle-password" onclick="togglePassword('registerPassword')" style="background: none; border: none;">
                    <span class="eye-icon">👁️</span>
                </button>
            </div>
            
            <div class="password-input-container">
                <input type="password" id="confirmPassword" name="confirmPassword" class="textbox-border" placeholder="Confirm Password"
                    required>
                <button type="button" class="toggle-password" onclick="togglePassword('confirmPassword')" style="background: none; border: none;">
                    <span class="eye-icon">👁️</span>
                </button>
            </div>
            <label for="avatar" class="avatarlabel">Avatar:</label>
            <input type="file" id="avatar" name="avatar" accept="image/*" class="textbox-border">
            <label for="description" class="descriptionlabel">Description:</label>
            <textarea id="description" name="description" placeholder="Optional"></textarea>

            <label for="securityQuestion" class="securitylabel">Security Question:</label>
            <select id="securityQuestion" name="securityQuestion" class="textbox-border" required>
                <option value="">Select a security question</option>
                <option value="What was your first pet's name?">What was your first pet's name?</option>
                <option value="In what city were you born?">In what city were you born?</option>
                <option value="What was your mother's maiden name?">What was your mother's maiden name?</option>
                <option value="What was the name of your first school?">What was the name of your first school?</option>
                <option value="What is your favorite movie?">What is your favorite movie?</option>
                <option value="What is your favorite food?">What is your favorite food?</option>
            </select>

            <input type="text" id="securityAnswer" name="securityAnswer" class="textbox-border"
                placeholder="Security Answer" required>

            <button type="submit">Register Now</button>
        </form>
    </div>
</div>

{{#if showLastLoginModal}}
<!-- Last Login Modal -->
<div id="lastLoginModal" class="modal-overlay">
  <div class="last-login-modal-content">
    <div class="modal-header">
      <h3>Welcome Back!</h3>
      <button class="modal-close" onclick="closeLastLoginModal()">&times;</button>
    </div>
    <div class="modal-body">
      {{#if lastLoginInfo.isFirstLogin}}
        <p>This appears to be your first login. Welcome to Restaurant Reviews!</p>
      {{else}}
        <p><strong>Last Login Activity:</strong></p>
        <div class="login-info">
          <p><strong>Last login attempt:</strong> {{formatDate lastLoginInfo.timestamp}}</p>
          <p><strong>Status:</strong> 
            {{#if lastLoginInfo.success}}
              <span style="color: #28a745; font-weight: bold;">✓ Successful</span>
            {{else}}
              <span style="color: #dc3545; font-weight: bold;">✗ Unsuccessful</span>
            {{/if}}
          </p>
          {{#if lastLoginInfo.errorMessage}}
            <p><strong>Error:</strong> {{lastLoginInfo.errorMessage}}</p>
          {{/if}}
        </div>
      {{/if}}
    </div>
    <div class="modal-footer">
      <button class="modal-button" onclick="closeLastLoginModal()">Continue</button>
    </div>
  </div>
</div>
{{/if}}



{{#if error_msg.[0]}}
{{#if (contains error_msg.[0] "Account locked")}}
<script>
    alert("{{error_msg.[0]}}");
</script>
{{/if}}
{{/if}}

{{/if}}

<script>
    function togglePassword(inputId) {
        const input = document.getElementById(inputId);
        const button = input.parentElement.querySelector('.toggle-password');
        const eyeIcon = button.querySelector('.eye-icon');
        
        if (input.type === 'password') {
            input.type = 'text';
            button.classList.add('active');
            eyeIcon.textContent = '🙈';
        } else {
            input.type = 'password';
            button.classList.remove('active');
            eyeIcon.textContent = '👁️';
        }
    }

    function validateRegisterForm() {
        const password = document.getElementById('registerPassword').value;
        const confirmPassword = document.getElementById('confirmPassword').value;
        
        if (password !== confirmPassword) {
            alert('Passwords do not match. Please try again.');
            return false;
        }
        
        return true;
    }

    function showModal(modalId) {
        var modal = document.getElementById(modalId);
        modal.style.display = "block";
    }

    function closeModal(modalId) {
        var modal = document.getElementById(modalId);
        modal.style.display = "none";
    }

    // Auto-open login modal if there is a login error
    window.onload = function() {
      var hasLoginError = {{#if error_msg}}true{{else}}false{{/if}};
      var showRegister = {{#if showRegister}}true{{else}}false{{/if}};
      if (showRegister) {
        showModal('registerModal');
      } else if (hasLoginError) {
        showModal('loginModal');
      }
    };
</script>